spring:
  datasource:
    url: jdbc:h2:file:~/village-data-dev;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
      #ddl-auto: create-drop
    #properties:
    #  hibernate:
    #    default_schema: village
logging:
  level:
    root: info
